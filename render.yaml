services:
  - type: web
    name: sentiment-face-app
    runtime: docker
    region: oregon
    plan: free
    envVars:
      - key: DEBUG
        value: "false"
      - key: CORS_ORIGINS
        value: '["*"]'
    healthCheckPath: /health
